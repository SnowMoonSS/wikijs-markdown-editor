// Stub for image-size type detector (Node.js only, not needed in browser)
// Used by markdown-it-imsize to auto-detect image dimensions

export default function detector() {}
export function disableFS() {}
export function lookup() { return null }
